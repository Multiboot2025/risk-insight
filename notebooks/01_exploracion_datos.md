# 01 — Exploración de datos

Análisis exploratorio de las tablas `siniestros`, `asegurados`, `polizas`, `proveedores`.

## Variables clave
- Distribución por ramo y ciudad.
- Distribución de `score_riesgo` y `nivel_riesgo`.
- Correlación entre `monto_reclamado / suma_asegurada` y nivel de riesgo.
- Reincidencia (`reclamos_ult_12m`) por segmento de asegurado.

## Hallazgos (placeholder)
Completar con resultados reales tras ejecutar los queries de `src/routes/_authenticated/dashboard.tsx`.
