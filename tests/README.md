# tests/

Pruebas unitarias y de integración. Sugerido: Vitest (`bunx vitest run`).

- `rules/` — pruebas del motor de reglas (`src/lib/fraud-rules.ts`).
- `services/` — pruebas de servicios (Supabase, IA).
- `components/` — pruebas de componentes UI.
